# Pre-commit hooks for mkdocs-editor-notes
# See https://pre-commit.com for more information

repos:
  # Run the full QA suite
  - repo: local
    hooks:
      - id: qa-full
        name: Run full QA suite
        entry: make qa/full
        language: system
        pass_filenames: false
        always_run: true
